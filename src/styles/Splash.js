import {StyleSheet} from 'react-native';
import Colors from '../values/Colors';

export default StyleSheet.create({
    splashImage: {
        width: '100%',
        height: 350,
        resizeMode: 'contain'
    }
});